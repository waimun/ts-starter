{
  "name": "ts-starter",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "ts-standard": "^12.0.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "lint": "ts-standard",
    "compile": "tsc",
    "test": "jest",
    "build": "npm run lint && npm run compile && npm test"
  }
}
