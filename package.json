{
  "name": "ts-starter",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "ts-standard": "^12.0.1",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "lint": "ts-standard",
    "compile": "tsc",
    "test": "jest",
    "build": "npm run lint && npm run compile && npm test"
  }
}
